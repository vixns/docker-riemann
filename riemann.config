; -*- mode: clojure; -*-
; vim: filetype=clojure:ts=2:sw=2

(logging/init)
(load-plugins)
(include "/etc/riemann/config")

(let [host "0.0.0.0"]
  (tcp-server {:host host})
  (udp-server {:host host})
  (ws-server  {:host host}))